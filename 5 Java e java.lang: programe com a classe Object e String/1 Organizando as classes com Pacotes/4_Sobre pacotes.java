// Veja as afirmações sobre Packages:

// a) Packages são uma forma de distribuir o código Java entre projetos.

// b) Packages são diretórios com significado especial dentro do código fonte
// Java

// c) A palavra chave package deve ser a primeira declaração no código fonte
// Java.

// d) Packages servem para organização e agrupar as classes e interfaces.

// Quais afirmações são verdadeiras?

// A)
// b,c,d
// Correto, pois packages são diretórios que tem significado especial dentro do
// código fonte Java (b), a palavra chave package deve ser a primeira declaração
// (c) e packages servem para organização e agrupar as classes e interfaces (d).
// Apenas a afirmação (a) é errada.

// B)
// a e c
// Errado, a afirmação (a) é errada e faltam as afirmações (b) e (d).
// Resumindo packages são diretórios que tem significado especial dentro do
// código fonte Java (b), a palavra chave package deve ser a primeira declaração
// (c) e packages servem para organização e agrupar as classes e interfaces (d).
// Apenas a afirmação (a) é errada.

// C)
// a, b, d
// Errado, a afirmação (a) é errada e falta a afirmação (c).
// Resumindo packages são diretórios que tem significado especial dentro do
// código fonte Java (b), a palavra chave package deve ser a primeira declaração
// (c) e packages servem para organização e agrupar as classes e interfaces (d).
// Apenas a afirmação (a) é errada.

// D)
// Todas
// Errado, a afirmação (a) é errada.
// Resumindo packages são diretórios que tem significado especial dentro do
// código fonte Java (b), a palavra chave package deve ser a primeira declaração
// (c) e packages servem para organização e agrupar as classes e interfaces (d).