// Veja a estrutura seguinte:

// src
// br
// gov
// rio
// sicis
// Usuario.java

// E o código fonte da classe Usuario:

// //AQUI

// public class Usuario {

// //atributos e métodos omitidos
// }

// Assumindo que a pasta src é a raiz do nosso código fonte. O que devemos
// colocar no lugar de //AQUI para compilar a classe Usuario com sucesso?

// A)
// package sicis.Usuario;
// Errado, o package deve conter todos os nomes dos diretórios separado pelo
// caracter . (ponto) e sem o nome da classe:
// package br.gov.rio.sicis;

// B)
// package Usuario;
// Errado, o package deve conter todos os nomes dos diretórios separado pelo
// caracter . (ponto) e e sem o nome da classe:
// package br.gov.rio.sicis;

// C)
// package br.gov.rio.sicis;
// Correto, o package possui todos os nomes dos diretórios separado pelo
// caracter . (ponto).

// D)
// package br.gov.rio.sicis.Usuario;
// Errado, o package possui apenas os nomes dos diretórios separado pelo
// caracter . (ponto) e sem o nome da classe:
// package br.gov.rio.sicis;