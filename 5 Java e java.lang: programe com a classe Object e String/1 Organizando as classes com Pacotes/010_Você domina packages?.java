// No desenvolvimento em Java, organizamos nossas classes em pacotes. Sobre esse
// tema é correto afirmar que:

// A)
// Seu uso só é válido em projetos com mais de 100 classes, já que a
// visualização de até 100 classes é feita de maneira trivial no Eclipse.
// Errado!

// B)
// O modificador default do Java restringe acesso a nível de pacote. Logo, se
// não for definido algum modificador, seja na classe, método, ou atributo,
// apenas classes do mesmo pacote podem acessar essas informações.
// Correto, com default apenas classes do mesmo pacote podem acessar essas
// informações.

// C)
// Quando um projeto utiliza pacotes, podemos usar o import de outras classes
// para sua utilização.
// Correto! O import facilita o uso da classe pois não precisamos do Full
// Qualified Name.

// D)
// Por organização e pela convenção adotada, precisamos seguir o domínio da
// empresa. Ou seja, se a empresa tem como domínio alura.com.br, os pacotes
// devem ser subpacotes de br.com.alura.
// Correto, mas existem exceções (poucas).