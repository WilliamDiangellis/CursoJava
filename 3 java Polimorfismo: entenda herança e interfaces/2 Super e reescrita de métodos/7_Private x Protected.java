// Qual é a diferença entre private e protected?

// A)
// Só a própria classe enxerga atributos/métodos private, enquanto protected é
// visto pela própria classe mais as classes filhas.
// Correto, atributos e métodos protected podem ser visto pelas próprias classes
// e suas filhas... Porém, com private apenas a própria classe enxerga os
// atributos/métodos.

// B)
// Só a própria classe enxerga atributos/métodos protected, enquanto private é
// visto pela própria classe mais as classes filhas.
// Errado, pois o private é o modificador que permite o acesso apenas à classe,
// e não o protected.

// C)
// Nenhuma. Em ambos, os atributos/métodos são visíveis para todos.
// Errado, pois há sim uma pequena diferença fundamental entre os dois
// modificadores de acesso. Só a própria classe enxerga atributos/métodos
// private, enquanto protected é visto pela própria classe mais as classes
// filhas.