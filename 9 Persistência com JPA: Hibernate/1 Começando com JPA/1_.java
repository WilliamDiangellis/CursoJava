// Olá, pessoal! Boas-vindas ao curso Persistência com JPA: Introdução ao Hibernate. Meu nome é
// Rodrigo Ferreira e serei o instrutor que acompanhará vocês durante este treinamento.

// Então, neste treinamento aprenderemos sobre a JPA, "Java Persistence API", poque ela veio para
// substituir - digamos assim - a JDBC, e aprender a utilizá-la na prática.

// Nosso treinamento será dividido em duas partes, e nesta parte discutiremos alguns tópicos. O primeiro é
// Motivação para utilizar JPA: o que é a JPA, por que foi criada e para resolver quais problemas. Enfim,
// a intenção é saber a utilização e motivação principal da JPA. O segundo é Download e Configurações:
// como fazer para baixar a JPA e utilizá-la no projeto (usando o hibernate como implementação).

// O terceiro é Arquivo persistence.xml: se refere às configurações. Aprenderemos para que serve o aquivo
// persistence.xml e como configurar os elementos da JPA nesse arquivo. O quarto é Mapeamento de entidades:
// classes que mapearão a tabela no banco de dados, ou seja, aprenderemos o que são essas entidades e como
// mapear de acordo com o banco de dados.

// O quinto é Mapeamento de relacionamentos: uma entidade que precisa se relacionar com outra, num
// relacionamento um para um, um para muitos, muitos para muitos, enfim, aprenderemos como fazer o
// mapeamento correto utilizando a JPA.

// O sexto é Ciclo de vida de uma entidade: quando mandamos salvar no banco de dados ou quando fazemos uma
// consulta, o que acontece com a entidade se mexemos no atributo, se será propagado para o banco de dados.
// O sétimo e último tópico é Consultas com JPQL: a JPQL é uma linguagem parecida com a SQL, mas que conta
// com a orientação a objetos.

// Então, estes são os principais tópicos que trataremos nesta primeira parte do treinamento de JPA. No
// primeiro vídeo, conheceremos um pouco da motivação e ideia da JPA. Vejo vocês lá!!
