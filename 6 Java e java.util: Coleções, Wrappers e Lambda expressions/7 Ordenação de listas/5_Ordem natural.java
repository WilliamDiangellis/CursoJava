// No contexto da aula, o que significa ordem natural?

// A)
// É a ordem definida pelo próprio elemento da lista.
// Correto, no nosso exemplo a classe Conta define o seu próprio critério de ordenação implementando a interface java.lang.Comparable.

// Nesse caso não é utilizado nenhum Comparator.

// B)
// É a ordem usada sem a definição de nenhum critério de ordenação
// Errado, sempre tem que ter algum criterio de ordenação. Ou é utilizado um a interface java.lang.Comparable (ordem natural) ou java.util.Comparator.

// C)
// É a ordem alfabética ou numérica
// Errado, a ordem natural é definida pelo próprio elemento da lista através da interface java.lang.Comparable.

// D)
// É a ordem definida pelo primeiro atributo do elemento da lista
// Errado, a ordem natural é definida pelo próprio elemento da lista através da interface java.lang.Comparable.