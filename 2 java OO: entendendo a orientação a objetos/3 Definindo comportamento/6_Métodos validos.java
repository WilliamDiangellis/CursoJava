// Assumindo que cada método abaixo está dentro de uma classe, quais declarações
// estão válidas (compilam)?

// A)
// void deposita(double valor, int numero)
// Errado, faltou no corpo do método (usar {}).

// B)
// void deposita(){

// }
// Correto, definimos o retorno (nesse caso void), definimos o nome do método
// (nesse caso deposita) e definimos corretamente os parâmetros (nesse casos não
// temos nenhum parâmetro ) .

// C)
// deposita(double valor){

// }
// Errado, faltou definir o retorno (usar void nesse caso).

// D)
// void deposita(double valor, int numero){

// }
// Correto, definimos o retorno (nesse caso void), definimos o nome do método
// (nesse caso deposita) e definimos corretamente os parâmetros (nesse casos
// dois, double valor e int numero ) .